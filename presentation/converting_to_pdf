IMPORTANT: re-run all of the cells first, to make sure everything's fine.

Run:

jupyter nbconvert --to html notebook.ipynb

Then open the html file in your browser and save it as a pdf (Ctrl + P), 'P' for 'print'.
